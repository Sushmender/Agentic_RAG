# FastAPI + server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Pydantic
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Auth — use bcrypt directly (passlib 1.7.4 is incompatible with bcrypt>=4.0)
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# HTTP clients
httpx==0.27.2
aiofiles==24.1.0

# Vector DB
chromadb==0.5.15

# Redis (Upstash compatible)
redis==5.1.0

# Retries / resilience
tenacity==9.0.0

# Logging
structlog==24.4.0

# Database (persistent store)
aiosqlite==0.20.0

# Rate limiting
slowapi==0.1.9

# File type detection
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"

# Env
python-dotenv==1.0.1

# Hashing
cryptography==43.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Code quality
ruff==0.6.9

# Document Parsing
landingai-ade==1.18.0
